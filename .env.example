# Root shared env
NODE_ENV=development

# Backend API
BACKEND_PORT=3001

# Database (Postgres running via docker-compose)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/recrm

# Dummy AWS keys for LocalStack (safe for local dev)
AWS_ACCESS_KEY_ID=dummy
AWS_SECRET_ACCESS_KEY=dummy
AWS_REGION=us-east-1

# S3 (MinIO or LocalStack)
S3_BUCKET=recrm-local

# Mailhog SMTP
SMTP_HOST=localhost
SMTP_PORT=1025